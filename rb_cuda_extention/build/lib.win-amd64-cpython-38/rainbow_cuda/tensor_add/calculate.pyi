def calculate(n: int, iters: int) -> int:
    """_summary_

    Args:
        n (int): vector[2 ^ n, 1]
        iters (int): 2 ^ iters times of inter add

    Returns:
        int: _description_
    """
    pass